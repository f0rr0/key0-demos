pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx,json,md,yml,yaml,toml}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    check:
      run: bun run check
